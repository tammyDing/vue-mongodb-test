# vue-mongodb-test
> A Vue.js project

## Build Setup
``` bash
# install dependecies
npm install

# serve width hot reload at localhost:8081
npm run dev

# bulid for production with minification
npm run bild
```

waiting for ...
